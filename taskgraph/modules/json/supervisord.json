[
  {
    "name": "serviceLog",
    "inputs": ["terminal", "host", "service"],
    "defaultInput": "service",
    "values": {
      "title": "ssh f{host} supervisorctl tail -f {service}",
      "command": "ssh {host} sudo supervisorctl tail -f {service}"
    }
  }
]
